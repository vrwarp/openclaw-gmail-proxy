"""OpenClaw Gmail Proxy -- a category-scoped Gmail enforcement layer.

The proxy holds the real Gmail OAuth token and exposes a narrow, category-scoped
MCP tool surface to an (untrusted) OpenClaw agent running in a separate VM.
"""

__version__ = "0.1.0"
